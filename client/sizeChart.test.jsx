// import SizeChart from './Components/SizeChart';

// test('expect SizeChart to have a table', () => {

// });

const sum = require('../sum');

test('sizechart', () => {
  expect(sum(1, 2)).toBe(3);
});
